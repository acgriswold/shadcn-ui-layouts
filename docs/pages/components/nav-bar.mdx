# Navigation

import { Callout, Tabs } from "nextra/components";

import { MobileNav, NavBarItem, Icons } from "shadcn-ui-layouts";
import "shadcn-ui-layouts/dist/style.css";

import { config } from "../../config/docs-config";

import * as React from "react";

## NavBar Item

<Tabs items={["variant", "opacity", "disabled", "asChild"]}>
  <Tabs.Tab>
    <NavBarItem variant="default" href={config.project.github}>
      Default
    </NavBarItem>

    ```tsx copy /variant/
       <NavBarItem variant="default" href={config.project.github}>
            Default
        </NavBarItem>
    ```

    <NavBarItem variant="subtle" href={config.project.github}>
      Subtle
    </NavBarItem>

    ```tsx copy /variant/
       <NavBarItem variant="subtle" href={config.project.github}>
            Subtle
        </NavBarItem>
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    <NavBarItem opacity="default" href={config.project.github}>
      Default
    </NavBarItem>

    ```tsx copy /opacity/
       <NavBarItem opacity="default" href={config.project.github}>
            Default
        </NavBarItem>
    ```

    <NavBarItem opacity="subtle" href={config.project.github}>
      Subtle
    </NavBarItem>

    ```tsx copy /opacity/
       <NavBarItem opacity="subtle" href={config.project.github}>
            Subtle
        </NavBarItem>
    ```

    <NavBarItem opacity="muted" href={config.project.github}>
      Muted
    </NavBarItem>

    ```tsx copy /opacity/
       <NavBarItem opacity="muted" href={config.project.github}>
            Muted
        </NavBarItem>
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    <NavBarItem disabled href={config.project.github}>
      Disabled
    </NavBarItem>

    ```tsx copy /disabled/
       <NavBarItem disabled href={config.project.github}>
            Disabled
        </NavBarItem>
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    <NavBarItem variant="default" opacity="subtle" asChild>
      [as Child]({config.project.github})
    </NavBarItem>
    
    ```tsx copy /asChild/
        <NavBarItem variant="default" opacity="subtle" asChild>
            [as Child]({config.project.github})
        </NavBarItem>
    ```
  </Tabs.Tab>
</Tabs>

## NavBar

<Callout type="info" emoji={<Icons.help />}>
  Please get hooks to work.
</Callout>
