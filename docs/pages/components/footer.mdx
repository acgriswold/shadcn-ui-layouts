# Footer

import { SimpleFooter, Icons, Button } from "shadcn-ui-layouts";
import "shadcn-ui-layouts/dist/style.css";

import { config } from "../../config/docs-config";

## Simple Footer

<SimpleFooter 
    logo={<Icons.logo />}
    pin={<Button variant="ghost" size="icon"><Icons.moon /></Button>}
    >
    <div className="flex flex-row mt-0">
        <div className="nx-mr-2">Built by</div>
        <a
            href={config.project.creator}
            target="_blank"
            rel="noreferrer"
            className="font-medium underline underline-offset-4"
        >
            @adam
        </a>
        <div className="nx-mr-2">. The source code is available on</div>
        <a
            href={config.project.github}
            target="_blank"
            rel="noreferrer"
            className="font-medium underline underline-offset-4"
        >
            GitHub
        </a>
    </div>
</SimpleFooter>
